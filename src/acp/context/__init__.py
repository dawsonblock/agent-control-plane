"""Context building (Milestone 6 — Haystack retrieval). Reserved.

Will scan repos + vault, index via haystack-ai, and emit a focused
context_bundle.md before the agent runs. Currently the graph's
build_context node is a prompt-only stub.
"""
