interface:
  display_name: "Agent Control Plane"
  short_description: "Repo-specific patterns and workflows for agent-control-plane"
  default_prompt: "Use the agent-control-plane repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true